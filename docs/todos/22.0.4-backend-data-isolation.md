# 📋 Tarefa 22.0.4 — Isolar dados de referência em backend/data/

## Objetivo
Mover `memorial_de_calculo_balanco.json` e as planilhas Excel da raiz do backend para subpastas de `backend/data/`.

## Arquivos Afetados
- `backend/memorial_de_calculo_balanco.json` ➔ `backend/data/memorial_de_calculo_balanco.json`
- `backend/MEMORIAL CALCULO.xlsx` ➔ `backend/data/reference/MEMORIAL CALCULO.xlsx`

## Caminhos a Atualizar
- `backend/src/main.py` e `backend/src/db/seeding.py` (ou onde quer que a semente JSON seja carregada no startup).
- `backend/scripts/convert_xlsx_to_json.py` (caminho da planilha de entrada Excel e arquivos de saída).
- `backend/scripts/export_database_to_json.py` (caminho de escrita do JSON do backend).
- `backend/src/services/services.py` (se referenciar o arquivo JSON).
- `frontend/src/hooks/useHarvestPlanState.ts` (ou qualquer outro arquivo que referencie `memorial_de_calculo_balanco.json`, wait, frontend/public/ ainda precisa ter o arquivo no local público para fallback offline, o qual é gerado pelos scripts).

## Plano de Execução
1. Criar pastas `backend/data/` e `backend/data/reference/`.
2. Mover arquivos usando `git mv`.
3. Fazer grep por `memorial_de_calculo_balanco.json` e `MEMORIAL CALCULO.xlsx` para encontrar referências no backend.
4. Ajustar referências encontradas.
5. Rodar o pytest para garantir que não quebrou nada.

## Verificação
- `pytest` passa 100%.
