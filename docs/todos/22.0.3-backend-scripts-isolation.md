# 📋 Tarefa 22.0.3 — Isolar scripts utilitários em backend/scripts/

## Objetivo
Mover os scripts utilitários que estão na raiz de `backend/` para `backend/scripts/` e documentá-los em `backend/scripts/README.md`.

## Arquivos Afetados
- `backend/convert_xlsx_to_json.py` ➔ `backend/scripts/convert_xlsx_to_json.py`
- `backend/export_database_to_json.py` ➔ `backend/scripts/export_database_to_json.py`
- `backend/scan_excel_groups.py` ➔ `backend/scripts/scan_excel_groups.py`
- `backend/scan_excel_turbinas.py` ➔ `backend/scripts/scan_excel_turbinas.py`

## Criações
- `backend/scripts/README.md` — Documentar o propósito e comando de execução de cada script.

## Plano de Execução
1. Criar pasta `backend/scripts/` (caso não exista, mas ela já existe? Let's check. Yes, it already exists, or we will create it if needed).
2. Mover arquivos de scripts via `git mv`.
3. Criar `backend/scripts/README.md`.
4. Atualizar qualquer referência ou import se necessário (os scripts geralmente importam da raiz ou usam python path, let's review them).
5. Validar a execução básica de cada script (dry run ou ajuda) para garantir que continuam funcionando.

## Verificação
- Os scripts podem ser invocados como `python backend/scripts/<nome>.py` e não geram erros de import.
